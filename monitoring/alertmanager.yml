global:
  resolve_timeout: 5m

route:
  receiver: 'discord-alerts'
  group_by: ['alertname', 'severity']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: 'discord-alerts'
    webhook_configs:
      - url: 'http://discord-relay:9095/'
        send_resolved: true
